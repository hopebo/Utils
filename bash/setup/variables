# Default editor triggered by other applications
export EDITOR="emacsclient -nw"
# Create emacs daemon server automatically if server doesn't exist
export ALTERNATE_EDITOR=""

# Set locale and character set
export LANG="en_US.UTF-8"
